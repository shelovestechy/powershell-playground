# Learning log

This is a short record of what changed and what I learned. It isn't a daily streak and it doesn't need an entry for every commit.

## 2026-08-25

- Replaced fictional Entra ID input files with real Microsoft Graph PowerShell read commands.
- Learned that shared mailbox permissions belong to Exchange Online PowerShell instead of only Entra ID.
- Added separate checks for `FullAccess`, `SendAs` and `SendOnBehalf`.
- Built a group comparison that uses two live Entra ID groups without an input CSV.
- Split the repository into learning tracks so it can grow without turning the main README into a small novel.
- Added traffic lights to separate read-only queries, local output and environment changes.

The next planned areas are PowerShell basics, Windows device checks and software troubleshooting.
